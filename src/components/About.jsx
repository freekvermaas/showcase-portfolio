import { memo } from "react"

const About = memo(function About() {
  return (
    (<div
      className="flex-shrink-0 w-screen h-screen snap-start bg-black text-slate-50 p-8 flex flex-col justify-center items-center">
    </div>)
  );
})

export default About


import { memo } from "react"

const Gallery = memo(function Gallery() {
  return (
    (<div
      className="flex-shrink-0 w-screen snap-start bg-white text-slate-950 overflow-y-auto">
    </div>)
  );
})

export default Gallery

